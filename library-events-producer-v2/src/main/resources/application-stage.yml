spring:
  kafka:
    bootstrap-servers: kafka-stage.example.com:9092
    producer:
      acks: all
      retries: 5
      properties:
        enable.idempotence: true
        linger.ms: 20
        retry.backoff.ms: 200
