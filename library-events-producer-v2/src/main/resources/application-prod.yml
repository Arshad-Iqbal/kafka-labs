spring:
  kafka:
    bootstrap-servers: kafka-prod.example.com:9092
    producer:
      acks: all
      retries: 10
      properties:
        enable.idempotence: true
        linger.ms: 50
        retry.backoff.ms: 500
